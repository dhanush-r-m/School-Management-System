{
  "name": "school-management-frontend",
  "version": "1.0.0",
  "description": "Frontend for School Management System",
  "scripts": {
    "start": "live-server --port=3001",
    "build": "echo 'No build step for plain HTML/CSS/JS project'",
    "test": "echo 'No tests configured'",
    "lint": "eslint js/",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "eslint": "^8.45.0",
    "live-server": "^1.2.2",
    "prettier": "^2.8.8"
  },
  "dependencies": {
    "axios": "^1.5.0"
  }
}